1. 开启 8888 端口， 服务进程启动时，会有一个不知名的 client 连上去
2. 有时候启动后，客户端连上并发了消息，但服务器好像没反应 / 服务器开启一段时间后, 再连上客户端，不会再有反应了? 是 windows 的问题?
   linux下没有问题, 但linux下出现运clinet发消息丢包问题
3. 配置了开8条线程, 但 gdb 后只发现有三条线程(linux下面没问题13条进程, 但windows下只有三条线程)
4. handler.message(msg, sz) msg需要调用接口去释放内存, 但总不能只有 netpack.to_string 这个选择吧? 如何传Lua?
5. 服务器回复客户端直接用socketdriver.send? 会不会有效率问题? 当发很大的包的时候
