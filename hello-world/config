local root = "./"
local skynet = root .. "skynet/"

app_name = "hello-world"
app_root = root .. app_name .. "/"

thread = 8

cpath = skynet .. "cservice/?.so"
harbor = 0

bootstrap = "snlua hello-world"

lua_path = skynet.."lualib/?.lua;"..app_root.."?.lua"
lua_cpath = skynet.."luaclib/?.so;"..app_root.."?.so"
luaservice = skynet.."service/?.lua;"..app_root.."?.lua"
lualoader = skynet.."lualib/loader.lua"
